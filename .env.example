DATABASE_URL="postgresql://user:password@host/db?sslmode=require"

# Secret key for hidden admin tools (kill switch + simulate). Set on Vercel for production.
MIS_ADMIN_SECRET="change-me-to-a-long-random-string"
